<section class="admin-dashboard">
<header class="page-header">
  <h1>Admin &raquo; Dashboard</h1>
</header>
<p>Below you will find a count of all products, and of each category. Click on the table heading to view the respective admin page. Totals are Displayed at the end of the table.</p>
<table class="table table-success table-hover">
  <thead>
    <tr>
      <th scope="col"><%= link_to 'Category', [:admin, :categories] %></th>
      <th scope="col"> <%= link_to 'Product Count', [:admin, :products] %></th>
    </tr>
   </thead>
   <tbody>
        <% @product_by_cat.each do |row| %>
    <tr>
      <td><%= row[0] %></td>
      <td><%= row[1] %></td>
    </tr>
      <% end %>
      <tfoot>
        <tr>
    <tr> 
      <td><strong> <%= @cat_count %></td>
      <td><strong><%= @product_count %> </td>
      </tr>
      </tfoot>
    </tbody>
</table>
</section>